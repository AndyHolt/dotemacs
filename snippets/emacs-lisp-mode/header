# name: Header for Elisp files
# key: header
# --
;;; `(insert (buffer-name))` --- ${1:Summary}

;; Author: Andy Holt (andrew.holt@hotmail.co.uk)
;; Date: `(insert (format-time-string "%a %d %b %Y %H:%M"))`
;; URL: ${2:URL}

;;; Commentary:
;;
;; ${3:Commentary}

;;; Code:

$0

(provide '`(insert (car (split-string (buffer-name) "\\.")))`)

;;; `(insert (buffer-name))` ends here
